---
// Empty frontmatter as no props are needed
---
<script>
    const originalTitle = document.title;
  
    document.addEventListener('visibilitychange', () => {
      if (document.hidden) {
        document.title = "brb";
      } else {
        document.title = originalTitle;
      }
    });
</script>
